#!/usr/bin/env node
/**
 * Legacy news JSON sanitization removed — news is managed in Supabase admin.
 * This script is kept as a no-op so existing npm scripts do not break.
 */
console.log("News is stored in Supabase. No JSON news files to sanitize.");
